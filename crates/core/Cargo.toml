[package]
name = "aghub-core"
version.workspace = true
edition.workspace = true
authors.workspace = true
license.workspace = true

[dependencies]
aghub-agents = { path = "../agents" }
serde = { workspace = true }
serde_json = { workspace = true }
serde_yaml = { workspace = true }
thiserror = { workspace = true }
dirs = { workspace = true }
toml = { workspace = true }
tempfile = { workspace = true, optional = true }
skill = { path = "../skill" }
chrono = { version = "0.4" }
which = "8"
log = "0.4.31"

[dev-dependencies]
aghub-agents = { path = "../agents" }
tempfile = { workspace = true }
libc = { workspace = true }
serde_json = { workspace = true }
# Integration tests seed the skill lock to exercise `accept_rename`'s
# lock-managed precondition; `skill` is already a normal dependency.
skill = { path = "../skill" }

[features]
default = [ "testing" ]
agent-validation = [  ]
testing = [ "tempfile" ]
