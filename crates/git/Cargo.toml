[package]
name = "aghub-git"
version.workspace = true
edition.workspace = true
authors.workspace = true
license.workspace = true
repository.workspace = true
description = "Git clone with credential injection from environment variables"

[dependencies]
gix = { version = "0.84", default-features = false, features = [
	"blocking-network-client",
	"worktree-mutation",
	"blocking-http-transport-reqwest-native-tls",
] }
gix-hash = { version = "0.25", default-features = false, features = [
	"sha1",
] }
log = "0.4"
reqwest = { workspace = true, features = ["blocking"] }
serde_json = { workspace = true }
tempfile = { workspace = true }
thiserror = { workspace = true }
url = { workspace = true }

[dev-dependencies]
tempfile = { workspace = true }
# Ground-truth Source-hash for the staging-materializer parity golden test.
skill = { path = "../skill" }
