# Dependencies
node_modules/
target/
dist/

# Lock files
*.lock
Cargo.lock
package-lock.json
yarn.lock
pnpm-lock.yaml

# Generated files
*.min.js
*.min.css
coverage/

# IDE
.vscode/
.idea/
*.swp
*.swo

# Claude Code scratch worktrees (transient checkouts of other branches — never
# lint them; they carry their own branch's files, including byte-exact fixtures).
.claude/worktrees/

# Session history scratch (git-ignored already, but prettier doesn't read
# .gitignore — keep these transient notes/dumps out of the format gate).
.remember/

# OS
.DS_Store
Thumbs.db

# Byte-exact skill test fixtures (hash-parity golden + npx-interop locks).
# These are hashed/compared verbatim; reformatting them breaks the golden tests.
crates/skill/tests/fixtures/

# Tauri
crates/desktop/src-tauri/gen/
crates/desktop/src-tauri/target/
crates/desktop/src-tauri/binaries/

# Logs
*.log
