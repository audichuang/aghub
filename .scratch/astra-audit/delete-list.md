# 待刪／待處理清單（已逐項用 preview 驗證）

驗證方式：每一項都跑過 `aghub-cli delete skills <n> -g --all-agents`（不加 `--yes` = 預覽），
或 `ls -L` 確認實體型態。分類依據是**實際的執行機制**，不是重要性。

---

## A. aghub-cli 管得到 — 一行指令清掉全部 agent 目錄

`aghub-cli` 會掃 15 個 agent 目錄，比手動 rm 乾淨。全部先跑預覽確認 `outcome`，再加 `--yes`。

| Skill                             | dlen | 為什麼刪                                                                                                             | 註記                                                                          |
| --------------------------------- | ---: | -------------------------------------------------------------------------------------------------------------------- | ----------------------------------------------------------------------------- |
| `cloudflare`                      |  368 | 13 個 Workers 專案重 grep 後 0 命中                                                                                  | 15 個 agent 目錄都有副本                                                      |
| `cloudflare-one`                  |  325 | Zero Trust／SASE 全機 0 命中                                                                                         |                                                                               |
| `cloudflare-one-migrations`       |  187 | 同上                                                                                                                 |                                                                               |
| `turnstile-spin`                  |  551 | `api/v4/accounts` 全機 0 命中                                                                                        | 帶 4 支腳本，還原成本較高                                                     |
| `cloudflare-email-service`        |  508 | 唯一命中在 skillsgate 的 `cloudflare-env.d.ts`，該檔第 2 行寫「Generated by Wrangler」= 自動生成的型別全集，不是使用 |
| `durable-objects`                 |  382 | 同上                                                                                                                 |
| `sandbox-next`                    |  352 | `@cloudflare/sandbox` 全機 0 命中                                                                                    |
| `sandbox-stable`                  |  351 | 同上                                                                                                                 |
| `sandbox-migrate-to-next`         |  278 | 同上                                                                                                                 |
| `writing-for-agents`              |  103 | 「寫 CLAUDE.md」的第 5 個 claimant，同時撞 skillgenie 的 create skills                                               | **會顯示 `kept`** → 之後手動 `rm -rf ~/.aghub/writing-for-agents`             |
| `skill-doctor`                    |  297 | skillgenie 22 次 vs 它 1 次，功能被涵蓋                                                                              | 刪完跑 `aghub-cli doctor`，若報 `orphanMaster` 手動清 `~/.aghub/skill-doctor` |
| `alpha`                           |   12 | 測試殘留，本文就一個字 `body`                                                                                        | 只在 `~/.agents/skills`，**真實 `~/.agents` 由你執行**                        |
| `verifying-inherited-root-causes` |  248 | 與 `validate-inherited-root-cause` 逐行 diff 只差一行 H1                                                             | **刪前先抄走它的 248 字元英文描述**，比留下的 483 字元版寫得好，階段 3 要用   |
| `yt-dlp-capture`                  |  399 | 併進 `agent-reach` 後才刪                                                                                            | **排在 agent-reach 收編之後**，順序不可反                                     |

小計：約 **4,461 字元**

```bash
# 預覽（安全，不會刪）
for n in cloudflare cloudflare-one cloudflare-one-migrations turnstile-spin \
         cloudflare-email-service durable-objects sandbox-next sandbox-stable \
         sandbox-migrate-to-next writing-for-agents skill-doctor; do
  echo "### $n"; aghub-cli delete skills $n -g --all-agents; done

# 確認無誤後才加 --yes
```

---

## B. aghub 拒絕 — 必須手動 rm（實測三支都被拒）

拒絕訊息：`this agent still reads it from a master shared with other agents`。
原因是 Master 在 `~/.skillshub`，不在 aghub 的 store，所以 aghub 不負責。

| Skill                       | dlen | 動作                                                              | 為什麼刪                                                                                                                                                                |
| --------------------------- | ---: | ----------------------------------------------------------------- | ----------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| `mole-cleanup`              |  307 | `rm ~/.claude/skills/mole-cleanup`                                | 描述自承需 macOS + `mo`；實測 `uname -s`=Linux、`command -v mo` 無輸出                                                                                                  |
| `openclaw-plugin-dev-skill` |  451 | `rm ~/.claude/skills/openclaw-plugin-dev-skill`                   | `~/github/openclaw` 最後 commit 2026-03-02，半年沒動                                                                                                                    |
| `brainstorming`             |  173 | `rm -f ~/.claude/skills/brainstorming ~/.skillshub/brainstorming` | 第 22 行「You are **not allowed** to implement, code, or modify behavior while this skill is active」正是文章第 5 點說會讓 Astra 提早停手的語氣；入口與 `grilling` 重疊 |

小計：約 **931 字元**

⚠️ `brainstorming` **只拆 symlink，不要動 `~/.skillshub/.repos/` 裡的 clone**，否則下次更新會還原。
⚠️ 這三支的 Master 留在 `~/.skillshub`，隨時可 `ln -s` 接回來。

---

## C. git 追蹤 — 要 git rm，會進版控

| 目標                       | 動作                                 | 為什麼                                                                                                                                                                                                                                                                                                                                                                                               |
| -------------------------- | ------------------------------------ | ---------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| `.claude/skills/init-deep` | `git rm -r .claude/skills/init-deep` | 它第 129/132/244/245/246/249 行反覆要求 `ln -s AGENTS.md CLAUDE.md`、第 388 行把「沒有配對 CLAUDE.md」列為 Anti-Pattern，**與本 repo「every CLAUDE.md is a one-line `@AGENTS.md` import (a real file, not a symlink)」直接相反**——在這裡跑它會反向改掉既有約定。還寫死了 `task(subagent_type=...)`、`LspServers()` 這些兩個引擎都不存在的呼叫語法。功能由全域 `agents-md-architecture` 分支 A/B 接手 |

⚠️ **與正在進行的 15 個 `.tsx` 修改分開 commit。**

---

## D. Plugin 開關 — 不是刪檔，是 settings.json 的 true→false

實測 `~/.claude/settings.json` 有 18 個 enabledPlugins，其中 17 個 ON。

| Plugin                                      |         字元 | 層級                | 為什麼                                                                                                                                                                                                             |
| ------------------------------------------- | -----------: | ------------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------ |
| `claude-code-setup@claude-plugins-official` |          354 | 全域                | 0 次呼叫                                                                                                                                                                                                           |
| `andrej-karpathy-skills@karpathy-skills`    |          219 | 全域                | 0 次呼叫，且「avoid overcomplication / surgical changes」與正在運作的 ponytail 完全同義，兩條互搶同一次判斷                                                                                                        |
| `hookify@claude-plugins-official`           |   395 (5 條) | 全域                | 0 次呼叫；資料目錄 `~/.claude/plugins/data/hookify-claude-plugins-official` **實測是空的**——裝了幾個月一條規則都沒建過                                                                                             |
| `ui-ux-pro-max@ui-ux-pro-max-skill`         | 2,798 (7 條) | **專案層**          | 0 次呼叫；`ui-styling` 主打 shadcn/Radix，本 repo 是 HeroUI v3 + React Aria，技術棧相衝。只在 aghub 關掉                                                                                                           |
| `imagine@agent-fleet`                       |          980 | **專案層** 或改上游 | `imagine-prompts` 878 字元是所有 plugin 最長的描述，內容在講兩個 engine 的 prompt 差異——那是本文該講的事。marketplace 是你自己的 `audichuang/agent-fleet-cc`，**優先改上游描述**成一行，功能留著、其他機器同步受益 |
| `pyright-lsp` / `jdtls-lsp`                 |       0 描述 | 檢視                | 對 Rust + TypeScript 的 aghub 無用，代價是每 session 起 LSP server。**若你有別的 Java/Python repo 就留著**                                                                                                         |

小計：約 **4,746 字元**（Claude-only，對 Codex 零影響）

專案層請寫 `/home/audichuang/research/aghub/.claude/settings.local.json`（已存在、90 bytes、不進版控），
不要寫 `settings.json`（會 commit、影響其他貢獻者）。

⚠️ **不要順手 rm plugin 的舊版本快取**：12 個目錄裡有兩個時間戳相同，刪錯就是刪掉現行版本。停用後快取本來就不進 context。

---

## E. 降級不是刪 — 從全域搬到用得到的專案

| Skill                | dlen | 落點                                                         | 註記                                                                                                                 |
| -------------------- | ---: | ------------------------------------------------------------ | -------------------------------------------------------------------------------------------------------------------- |
| `agents-sdk`         |  431 | `~/research/honcho`（**repo root，不是 honcho/mcp 子目錄**） | 全機掃 import 只有 `honcho/mcp/src/index.ts` 一個消費者                                                              |
| `notebooklm`         |  307 | `~/research/audiskill/podcast-lab`                           | marker 齊全，已驗                                                                                                    |
| `synology-container` |  267 | `~/ops`                                                      | ⚠️ 該目錄**零 project marker**（沒有 `.claude/`、`.git`、`.mcp.json`），`-p` 必定 bail → 先 `mkdir -p ~/ops/.claude` |

⚠️ 降級指令**必須含 cline 與 warp**：`~/.agents/skills` 的全域寫入者是它們，而 Codex 同時讀
`.codex/skills` 與 `.agents/skills`，少了就等於 Codex 一字未省。
⚠️ **不要用 `-a all`**——實測 delete/add/update 都 REJECT 它，正確是逗號清單或 `--all-agents`。

---

## F. 待你裁決

| 對象                                                                           | 選項                                                                                                                    |
| ------------------------------------------------------------------------------ | ----------------------------------------------------------------------------------------------------------------------- |
| 21 支前端設計動詞包（proj `.claude/skills` + `.agents/skills`，約 5,236 字元） | A 加 `disable-model-invocation`／B 併成一支 `design-pass` router／C `git rm` 刪掉                                       |
| `grill-me` / `grill-with-docs`（本文只有 39B / 67B 的轉址 stub）               | Claude 端已有 `disable-model-invocation`、零成本；**Codex 端要不要刪，先觀察一次 Codex session 才知道它吃不吃這個旗標** |
| `heroui-react` 描述改寫                                                        | source 是 `heroui-inc/heroui`，改完永久 `update-available`、此後不可對它跑 `apply-update`                               |

---

## 合計

| 類別                 | 省下描述字元 | 受益        |
| -------------------- | -----------: | ----------- |
| A aghub-cli          |       ~4,461 | both        |
| B 手動 rm            |         ~931 | claude      |
| C git rm             |          387 | claude      |
| D plugin 開關        |       ~4,746 | claude only |
| E 降級               |       ~1,005 | both        |
| F 設計動詞（若選 C） |       ~5,236 | both        |
| **合計**             |  **~16,766** |             |

## 不要刪的（複查特別點名）

- `wrangler`、`workers-best-practices`：實測 `~/research` 底下有 **13 個 Workers 專案**
- `web-perf`：不是 Cloudflare skill，是 Chrome DevTools 的 Core Web Vitals 稽核
- `cloudflare-mcp-worker`：唯一被 aghub 納管的 CF skill，honcho/mcp、booking_badminton_mcp 都是 MCP-on-Workers
- `research`：`ask-matt` 的路由圖第 81 行明確指派它（只改描述撞 `agent-reach` 的那半句）
- `audit` vs `critique`、`quieter` vs `distill`、`colorize` vs `bolder` vs `overdrive`：各有不同執行手冊，不是分身
- 13 支帶 `disable-model-invocation` 的 matt-pocock skill：本來就沒計費，刪了只會少掉能 slash 呼叫的工具
- 專案 `skills-lock.json` 的 22 筆：帶 `source: pbakaus/impeccable` + `computedHash`，刪了就**永久查不出來源**，`check --online` / `source diff` / `apply-update` 全部失效
